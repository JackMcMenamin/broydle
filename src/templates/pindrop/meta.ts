import type { TemplateMeta } from "@/core/base";

export const PINDROP_META: TemplateMeta = {
  name: "Pin Drop",
  how: "Five places. Click the map to drop a pin, drag or click again to move it, then confirm. Land within the distance limit and it counts. Miss by more and you lose a heart.",
  example: "Where is the Eiffel Tower? Drop the pin on northern France.",
};
